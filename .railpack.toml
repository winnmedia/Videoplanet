# Railpack Configuration for VideoPlanet Django Backend

[providers]
python = "3.11"

[build]
cmd = "cd vridge_back && pip install --upgrade pip && pip install -r requirements.txt && python manage.py collectstatic --noinput --clear"

[start] 
cmd = "cd vridge_back && daphne -b 0.0.0.0 -p $PORT config.asgi:application"

[env]
DJANGO_SETTINGS_MODULE = "config.settings"
PYTHONPATH = "/app/vridge_back"
PYTHONUNBUFFERED = "1"
SECRET_KEY = "4(7b%fv3+tc0ptvzu9j2yh9)wotp(lty83a^b)d$79^+r(=trg"