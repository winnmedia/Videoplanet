/**
 * Shared Config Public API
 */

export {
  normalizeUrl,
  API_BASE_URL,
  SOCKET_URL,
  APP_URL,
  isDevelopment,
  isProduction,
  validateEnvironment,
  createApiUrl,
  createSocketUrl,
  isValidUrl,
  getEnvironmentInfo,
} from './env';