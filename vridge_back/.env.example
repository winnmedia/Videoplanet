# Django Settings
DEBUG=True
SECRET_KEY=your-secret-key-here
ALGORITHM=HS256

# SendGrid Email Configuration
SENDGRID_API_KEY=your-sendgrid-api-key-here
DEFAULT_FROM_EMAIL=noreply@vridge.kr
DEFAULT_FROM_NAME=Vlanet

# Database Settings (Production)
RDS_NAME=postgres
RDS_USER=vridge
RDS_PASSWORD=your-database-password
RDS_HOST=your-database-host

# AWS Settings (if using S3 for media storage)
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_STORAGE_BUCKET_NAME=your-bucket-name

# Sentry Configuration (for error tracking)
SENTRY_URL=your-sentry-dsn-url

# CORS and Security
ALLOWED_HOSTS=api.vridge.kr,127.0.0.1,localhost
CORS_ALLOWED_ORIGINS=https://vridge.kr,http://localhost:3000,https://vlanet.net

# Redis Configuration (for channels)
REDIS_HOST=127.0.0.1
REDIS_PORT=6379